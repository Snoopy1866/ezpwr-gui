启动项目：

```python
uv run python -m ezpwr_gui.main
```

打包项目：

```python
uv run python -m nuitka src/ezpwr_gui/main.py
```

## Windows

```bash

```

## Mac OS

```bash
--macos-app-version
```

## Linux

```bash
sudo apt install patchelf
sudo apt install clang
```
